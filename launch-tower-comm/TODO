# TODO

## Software Related

### Critical
* `on_startup` set shore power indicator
* LTCctrl: change button state on callback from ltcbackend
* LTCctrl: verify with hardware before changing state.
* Set logging directory
* Add numeric bounds to color changes in indicators - RR

### Non-critical
* Color-fy the buttons
* Add animations for state message changes
* Install on LTC: rsync, tmux, vim, strace
* Set default Rocket Commander window size.

## Hardware Related

### Critical


### Non-critical

* No sensor logging on LTC

# Next launch
* Do relays maintain state on power cycle?
* ignition relay watchdog
* Local phidgets service and our own network communication code
* Timestamps in logs
* Write to file for logging until Kivy stops breaking the Python logging
* full event driven front-end
* Calibrated sensor values
* Try to break the network
